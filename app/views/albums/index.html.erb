<p id="notice"><%= notice %></p>

<h1>Listing albums</h1>

<%= link_to 'New Album', new_album_path %>


    <div class="hashtag-footer">
      <a href="#" class="btn btn-info btn-mini">Make Album</a>
      <a href="#" class="btn btn-info btn-mini">View Photos</a>
    </div>  
    <br>

<% @album_data.each do |album| %>
  <%= link_to album[2], album_url(album[3]) %>
  <%= image_tag album[0]%>
<!--   <%= @album_data %> -->
  <p></p>
  <br>
	<%= link_to "Delete Album", album_url(album[1]), :method => "delete" %>
  <br>
<% end %>